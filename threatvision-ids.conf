# ==============================================================================
# ThreatVision AI (SentinelX SOC) — Configuration File
# ==============================================================================

# 🌐 NETWORK MONITORING INTERFACE
# The network interface to sniff on. 
# Options: 
#   - Name of interface (e.g. eth0, wlan0, wlo1, enp3s0)
#   - 'auto' to let the system automatically detect the default active route.
IDS_INTERFACE=auto

# 📢 TELEGRAM ALERTS BOT INTEGRATION (Optional)
# If configured, HIGH and CRITICAL alerts will be pushed to the specified chat ID.
# Leave empty or commented out to disable Telegram alerts.
TELEGRAM_TOKEN=8986675659:AAGTm0wrInBmyte9WGuLRBJ9rbCePEULHZg
TELEGRAM_CHAT_ID=8416266813

# 🍃 DATABASE CONFIGURATION (Optional)
# Default runs in local CSV fallback mode (saved under /opt/threatvision-ids/logs/).
# Uncomment and customize this URI to connect to a custom MongoDB server.
# MONGODB_URI=mongodb://localhost:27017/
